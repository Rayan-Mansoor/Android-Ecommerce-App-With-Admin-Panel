<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcommerceApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/orange_light</item>
    </style>

    <style name="Theme.EcommerceApp" parent="Base.Theme.EcommerceApp" >

        <item name="colorPrimary">@color/orange_light</item>

        <item name="android:navigationBarColor">@color/orange_light</item>

    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/orange_light</item>
        <item name="android:statusBarColor">@color/orange_light</item>
        <!-- ... other theme attributes -->
    </style>

</resources>